/**
 * SampleUserService라는 유즈케이스에 귀속된 DTO이므로
 * 같은 이름으로 폴더를 만들어 배치합니다.
 */
export type PostSampleLoginDto = {
  email: string;
  password: string;
};

export type SampleUserDto = {
  name: string;
};
